package tests;

import com.microsoft.playwright.Page;
import org.junit.jupiter.api.*;
import base.BaseTest;
import pages.LoginPage;
import io.qameta.allure.Description;
import io.qameta.allure.Epic;
import io.qameta.allure.Feature;
import io.qameta.allure.Story;

/**
 * LoginTest — Generated test cases for Login module
 * Generated by: Qentrix ScriptGenerationAgent
 * Framework: Playwright Java (Hybrid)
 * Test Inventory Cases: 562, 22, 24, 25, 21, 23, 561, 563, 560, 564
 */
@Epic("Authentication")
@Feature("Login")
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class LoginTest extends BaseTest {

    private LoginPage loginPage;

    @BeforeEach
    public void setup() {
        loginPage = new LoginPage(page);
    }

    // ── @smoke — Critical path tests ──────────────────────────────────────────

    @Test
    @Tag("smoke")
    @Tag("regression")
    @Tag("TC-21")
    @Order(1)
    @Story("TC-21: Verify that login works with valid credentials")
    @Description("Test case 21: Open login page, enter valid username and password, click login button, verify user login successful")
    public void tc021_verifyLoginWorksWithValidCredentials() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page loaded correctly", () -> {
            loginPage.verifyLoginPageLoaded();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterEmail("admin"); // TODO: replace with getLoginDataByRole("Admin") after credentials.csv is configured
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Click login button", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify user login successful", () -> {
            loginPage.verifyLoginSuccessful();
        });
    }

    @Test
    @Tag("smoke")
    @Tag("regression")
    @Tag("TC-560")
    @Order(2)
    @Story("TC-560: Verify that login works with valid credentials (API)")
    @Description("Test case 560: Send POST request to /login endpoint with valid username and password, validate 200 status code and authentication token")
    public void tc560_verifyLoginWorksWithValidCredentialsAPI() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterEmail("admin");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Validate response returns 200 status code", () -> {
            loginPage.verifyStatusCode(200);
        });

        Allure.step("Validate response contains authentication token", () -> {
            loginPage.verifyAuthTokenPresent();
        });
    }

    // ── @regression — Invalid credentials tests ───────────────────────────────

    @Test
    @Tag("regression")
    @Tag("TC-561")
    @Order(3)
    @Story("TC-561: Verify that login fails with invalid username")
    @Description("Test case 561: Send POST request to /login endpoint with invalid username and valid password, validate 401 status code and error message")
    public void tc561_verifyLoginFailsWithInvalidUsername() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter invalid username", () -> {
            loginPage.enterEmail("invaliduser@test.com");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Validate response returns 401 status code", () -> {
            loginPage.verifyStatusCode(401);
        });

        Allure.step("Validate error message indicates invalid credentials", () -> {
            loginPage.verifyErrorMessageContains("invalid credentials");
        });
    }

    @Test
    @Tag("regression")
    @Tag("TC-562")
    @Order(4)
    @Story("TC-562: Verify that login fails with invalid password")
    @Description("Test case 562: Send POST request to /login endpoint with valid username and invalid password, validate 401 status code and error message")
    public void tc562_verifyLoginFailsWithInvalidPassword() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterEmail("admin");
        });

        Allure.step("Enter invalid password", () -> {
            loginPage.enterPassword("wrongpassword123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Validate response returns 401 status code", () -> {
            loginPage.verifyStatusCode(401);
        });

        Allure.step("Validate error message indicates invalid credentials", () -> {
            loginPage.verifyErrorMessageContains("invalid credentials");
        });
    }

    @Test
    @Tag("regression")
    @Tag("TC-22")
    @Order(5)
    @Story("TC-22: To Test Login Form with Invalid Data")
    @Description("Test case 22: Navigate to 192.168.10.124:4001, verify login page displays, enter username and password")
    public void tc022_testLoginFormWithInvalidData() {
        Allure.step("Navigate to 192.168.10.124:4001", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page displays with Email, Password fields, Login button and Register hyperlink", () -> {
            loginPage.verifyLoginPageLoaded();
        });

        Allure.step("Enter username and password", () -> {
            loginPage.enterEmail("testuser@invalid.com");
            loginPage.enterPassword("invalidpass");
        });

        Allure.step("System should accept the username and password", () -> {
            // Input acceptance verified implicitly by fill success
        });
    }

    // ── @regression — Empty field tests ───────────────────────────────────────

    @Test
    @Tag("regression")
    @Tag("TC-563")
    @Order(6)
    @Story("TC-563: Verify that login fails with empty username")
    @Description("Test case 563: Send POST request to /login endpoint with empty username and valid password, validate 400 status code and missing username error")
    public void tc563_verifyLoginFailsWithEmptyUsername() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Leave username field empty", () -> {
            loginPage.enterEmail("");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Validate response returns 400 status code", () -> {
            loginPage.verifyStatusCode(400);
        });

        Allure.step("Validate error message indicates missing username", () -> {
            loginPage.verifyRequiredFieldError();
        });
    }

    @Test
    @Tag("regression")
    @Tag("TC-564")
    @Order(7)
    @Story("TC-564: Verify that login fails with empty password")
    @Description("Test case 564: Send POST request to /login endpoint with valid username and empty password, validate 400 status code and missing password error")
    public void tc564_verifyLoginFailsWithEmptyPassword() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterEmail("admin");
        });

        Allure.step("Leave password field empty", () -> {
            loginPage.enterPassword("");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Validate response returns 400 status code", () -> {
            loginPage.verifyStatusCode(400);
        });

        Allure.step("Validate error message indicates missing password", () -> {
            loginPage.verifyRequiredFieldError();
        });
    }

    // ── Placeholder tests (minimal step details provided) ─────────────────────

    @Test
    @Tag("regression")
    @Tag("TC-23")
    @Order(8)
    @Story("TC-23: To Test Login Form with Invalid Data")
    @Description("Test case 23: No steps provided — placeholder test")
    public void tc023_testLoginFormWithInvalidDataPlaceholder() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page loaded", () -> {
            loginPage.verifyLoginPageLoaded();
        });
        // TODO: Add test steps when test case 23 is fully defined
    }

    @Test
    @Tag("regression")
    @Tag("TC-24")
    @Order(9)
    @Story("TC-24: To Test Login Form with Invalid Data")
    @Description("Test case 24: No steps provided — placeholder test")
    public void tc024_testLoginFormWithInvalidDataPlaceholder() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page loaded", () -> {
            loginPage.verifyLoginPageLoaded();
        });
        // TODO: Add test steps when test case 24 is fully defined
    }

    @Test
    @Tag("regression")
    @Tag("TC-25")
    @Order(10)
    @Story("TC-25: test import 01")
    @Description("Test case 25: No steps provided — placeholder test")
    public void tc025_testImport01Placeholder() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page loaded", () -> {
            loginPage.verifyLoginPageLoaded();
        });
        // TODO: Add test steps when test case 25 is fully defined
    }
}
