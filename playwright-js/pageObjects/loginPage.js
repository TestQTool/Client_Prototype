// Login Page Object - Locators Only
// Generated by Qentrix for AG-Helix

// Form Fields
export const usernameInput = "input[type='text'][placeholder*='mail' i], input[type='email'], input[name='username'], input[name='email'], #username, #email, //label[contains(., 'Email')]/following::input[1], //label[contains(., 'Username')]/following::input[1]";
export const passwordInput = "input[type='password'], input[name='password'], #password, //label[contains(., 'Password')]/following::input[1]";

// Buttons
export const loginButton = "button[type='submit'], button:has-text('Login'), button:has-text('Sign In'), input[type='submit'], #login-button, //button[contains(., 'Login')]";

// Links
export const registerLink = "a:has-text('Register'), a[href*='register'], //a[contains(., 'Register')]";

// Error Messages
export const errorMessage = ".error-message, .alert-error, .validation-error, [role='alert'], .text-danger, //div[contains(@class, 'error')], //*[contains(text(), 'Invalid') or contains(text(), 'incorrect') or contains(text(), 'failed')]";
export const usernameRequiredError = ".username-error, .email-error, //label[contains(., 'Email')]/following::*[contains(@class, 'error')][1], //*[contains(text(), 'Username') and contains(text(), 'required')]";
export const passwordRequiredError = ".password-error, //label[contains(., 'Password')]/following::*[contains(@class, 'error')][1], //*[contains(text(), 'Password') and contains(text(), 'required')]";
export const accountDisabledError = "//*[contains(text(), 'disabled') or contains(text(), 'Disabled') or contains(text(), 'suspended')]";
export const rateLimitError = "//*[contains(text(), 'locked') or contains(text(), 'too many') or contains(text(), 'wait')]";

// Page Elements
export const loginPageTitle = "h1, .page-title, .login-title, //h1[contains(., 'Login') or contains(., 'Sign In')]";
export const brandingElements = ".logo, .brand, header, .app-name";

// Security Indicators
export const secureConnectionIndicator = "body"; // For HTTPS verification via page URL

// Loading States
export const loadingSpinner = ".spinner, .loading, [role='progressbar'], .loader";

// Placeholder Text Selectors
export const usernamePlaceholder = "input[placeholder*='mail' i], input[placeholder*='username' i]";
export const passwordPlaceholder = "input[type='password'][placeholder]";

// Network/API Request Monitoring (conceptual - actual monitoring done in test layer)
export const apiRequestIndicator = "body"; // API calls monitored via Playwright network interception

// Responsive Design Check Elements
export const responsiveContainer = ".login-container, .login-form, form, main";

// Field Validation Styling
export const invalidFieldStyle = ".is-invalid, .error, [aria-invalid='true'], input.error";
export const validFieldStyle = ".is-valid, .success, [aria-invalid='false']";

