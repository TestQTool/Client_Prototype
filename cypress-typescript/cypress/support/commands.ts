// cypress/support/commands.ts
// ─────────────────────────────────────────────────────────────────────────────
// Custom Cypress Commands
// Generated by: Qentrix ScriptGenerationAgent | Framework: Cypress TypeScript
// ─────────────────────────────────────────────────────────────────────────────

/// <reference types="cypress" />

declare global {
    namespace Cypress {
        interface Chainable {
            /**
             * Custom command to wrap test.step() style execution
             * @param stepName - Name of the step
             * @param fn - Function to execute
             */
            step(stepName: string, fn: () => void): Chainable<void>;
        }
    }
}

Cypress.Commands.add('step', (stepName: string, fn: () => void) => {
    cy.log(`**${stepName}**`);
    return cy.wrap(null).then(() => fn());
});

export {};
