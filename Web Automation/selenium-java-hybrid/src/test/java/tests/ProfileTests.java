package tests;

import base.BaseTest;
import org.junit.jupiter.api.*;
import pages.LoginPage;
import pages.ProfilePage;
import io.qameta.allure.*;

/**
 * ProfileTests.java
 * Generated test cases for Profile feature
 * Framework: selenium-java-hybrid | Generated by: ScriptGenerationAgent
 */
@Epic("User Management")
@Feature("Profile")
public class ProfileTests extends BaseTest {

    private LoginPage loginPage;
    private ProfilePage profilePage;

    @BeforeEach
    public void setup() {
        super.setup();
        loginPage = new LoginPage(driver);
        profilePage = new ProfilePage(driver);
    }

    @Test
    @Tag("smoke")
    @Tag("regression")
    @Tag("TC-596")
    @Story("TC-596: Verify that user can update profile information")
    @Description("Login and navigate to profile page, update first name field, update email address, click save changes button, verify success message appears")
    @Severity(SeverityLevel.CRITICAL)
    public void TC_596_VerifyUserCanUpdateProfileInformation() {
        Allure.step("Login and navigate to profile page", () -> {
            loginPage.navigate();
            loginPage.loginAsRole("Admin");
            profilePage.navigate();
            profilePage.verifyProfilePageLoaded();
        });

        Allure.step("Update first name field", () -> {
            profilePage.updateFirstName("UpdatedFirstName");
        });

        Allure.step("Update email address", () -> {
            profilePage.updateEmail("updated" + System.currentTimeMillis() + "@test.com");
        });

        Allure.step("Click save changes button", () -> {
            profilePage.clickSaveChanges();
        });

        Allure.step("Verify success message appears", () -> {
            profilePage.verifySuccessMessageDisplayed();
        });
    }

    @Test
    @Tag("regression")
    @Tag("TC-597")
    @Story("TC-597: Verify that profile update fails with invalid email format")
    @Description("Login and navigate to profile page, enter invalid email format, keep other fields valid, click save changes button, verify error message for email indicating invalid email format")
    @Severity(SeverityLevel.NORMAL)
    public void TC_597_VerifyProfileUpdateFailsWithInvalidEmailFormat() {
        Allure.step("Login and navigate to profile page", () -> {
            loginPage.navigate();
            loginPage.loginAsRole("Admin");
            profilePage.navigate();
            profilePage.verifyProfilePageLoaded();
        });

        Allure.step("Enter invalid email format", () -> {
            profilePage.updateEmail("invalid-email-format");
        });

        Allure.step("Keep other fields valid", () -> {
            profilePage.updateFirstName("ValidName");
        });

        Allure.step("Click save changes button", () -> {
            profilePage.clickSaveChanges();
        });

        Allure.step("Verify error message for email", () -> {
            profilePage.verifyInvalidEmailError();
        });

        Allure.step("Verify error indicates invalid email format", () -> {
            profilePage.verifyInvalidEmailError();
        });
    }

    @Test
    @Tag("smoke")
    @Tag("regression")
    @Tag("TC-606")
    @Story("TC-606: Verify that user can change password from profile settings")
    @Description("Login and navigate to profile settings, click change password option, enter current password, enter new password and confirm, save password changes, verify password is updated successfully")
    @Severity(SeverityLevel.CRITICAL)
    public void TC_606_VerifyUserCanChangePasswordFromProfileSettings() {
        Allure.step("Login and navigate to profile settings", () -> {
            loginPage.navigate();
            loginPage.loginAsRole("Admin");
            profilePage.navigate();
            profilePage.verifyProfilePageLoaded();
        });

        Allure.step("Click change password option", () -> {
            profilePage.clickChangePassword();
        });

        Allure.step("Enter current password", () -> {
            String[] creds = profilePage.getLoginDataByRole("Admin");
            profilePage.enterCurrentPassword(creds[1]);
        });

        Allure.step("Enter new password and confirm", () -> {
            profilePage.enterNewPassword("NewValidPass123!");
            profilePage.enterConfirmPassword("NewValidPass123!");
        });

        Allure.step("Save password changes", () -> {
            profilePage.clickSaveChanges();
        });

        Allure.step("Verify password is updated successfully", () -> {
            profilePage.verifySuccessMessageDisplayed();
        });
    }

    @Test
    @Tag("regression")
    @Tag("TC-607")
    @Story("TC-607: Verify that password change fails with incorrect current password")
    @Description("Login and navigate to profile settings, click change password option, enter incorrect current password, enter new password and confirm, verify error message appears indicating current password is incorrect")
    @Severity(SeverityLevel.NORMAL)
    public void TC_607_VerifyPasswordChangeFailsWithIncorrectCurrentPassword() {
        Allure.step("Login and navigate to profile settings", () -> {
            loginPage.navigate();
            loginPage.loginAsRole("Admin");
            profilePage.navigate();
            profilePage.verifyProfilePageLoaded();
        });

        Allure.step("Click change password option", () -> {
            profilePage.clickChangePassword();
        });

        Allure.step("Enter incorrect current password", () -> {
            profilePage.enterCurrentPassword("WrongPassword123!");
        });

        Allure.step("Enter new password and confirm", () -> {
            profilePage.enterNewPassword("NewValidPass123!");
            profilePage.enterConfirmPassword("NewValidPass123!");
        });

        Allure.step("Verify error message appears", () -> {
            profilePage.clickSaveChanges();
            profilePage.verifyIncorrectPasswordError();
        });

        Allure.step("Verify error indicates current password is incorrect", () -> {
            profilePage.verifyIncorrectPasswordError();
        });
    }

    @Test
    @Tag("smoke")
    @Tag("regression")
    @Tag("TC-610")
    @Story("TC-610: Verify that user can view account information")
    @Description("Login with valid credentials, navigate to account information page, verify username is displayed, verify email address is shown, verify account creation date")
    @Severity(SeverityLevel.NORMAL)
    public void TC_610_VerifyUserCanViewAccountInformation() {
        Allure.step("Login with valid credentials", () -> {
            loginPage.navigate();
            loginPage.loginAsRole("Admin");
        });

        Allure.step("Navigate to account information page", () -> {
            profilePage.navigate();
            profilePage.verifyProfilePageLoaded();
        });

        Allure.step("Verify username is displayed", () -> {
            String[] creds = profilePage.getLoginDataByRole("Admin");
            profilePage.verifyUsernameDisplayed(creds[0]);
        });

        Allure.step("Verify email address is shown", () -> {
            profilePage.verifyEmailDisplayed("");
        });

        Allure.step("Verify account creation date", () -> {
            profilePage.verifyCreationDateDisplayed();
        });
    }
}
