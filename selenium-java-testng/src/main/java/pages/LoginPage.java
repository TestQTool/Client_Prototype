package pages;

import base.BasePage;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import pageObjects.LoginPage as LoginPageLocators;
import static org.assertj.core.api.Assertions.assertThat;

/**
 * LoginPage — Actions and Assertions
 * Generated by Qentrix ScriptGenerationAgent
 * Framework: Selenium + Java + TestNG
 * Extends: BasePage for WebActions helpers and test data access
 */
public class LoginPage extends BasePage {

    public LoginPage(WebDriver driver) {
        super(driver);
    }

    // ── Navigation ────────────────────────────────────────────────────────────

    public void navigate() {
        String baseUrl = System.getenv("BASE_URL");
        if (baseUrl == null || baseUrl.isEmpty()) {
            baseUrl = getTestData("baseUrl", "http://192.168.10.124:4001");
        }
        open(baseUrl + LoginPageLocators.LOGIN_PATH);
        waitForPageLoad();
    }

    // ── Actions ───────────────────────────────────────────────────────────────

    public void enterUsername(String username) {
        waitAndSendKeys(getElement(LoginPageLocators.USERNAME_INPUT), username);
    }

    public void enterEmail(String email) {
        waitAndSendKeys(getElement(LoginPageLocators.EMAIL_INPUT), email);
    }

    public void enterPassword(String password) {
        waitAndSendKeys(getElement(LoginPageLocators.PASSWORD_INPUT), password);
    }

    public void clickLoginButton() {
        waitAndClick(getElement(LoginPageLocators.LOGIN_BUTTON));
        waitForPageLoad();
    }

    public void clickRegisterLink() {
        waitAndClick(getElement(LoginPageLocators.REGISTER_LINK));
        waitForPageLoad();
    }

    /**
     * Complete login flow with username and password
     */
    public void loginWithCredentials(String username, String password) {
        enterUsername(username);
        enterPassword(password);
        clickLoginButton();
    }

    /**
     * Login using role from credentials CSV
     */
    public void loginAs(String roleName) {
        String[] creds = getLoginDataByRole(roleName);
        String username = creds[0];
        String password = creds[1];
        enterUsername(username);
        enterPassword(password);
        clickLoginButton();
    }

    /**
     * Submit login without filling fields (for validation tests)
     */
    public void submitEmptyLogin() {
        clickLoginButton();
    }

    // ── Assertions ────────────────────────────────────────────────────────────

    public void verifyLoginPageLoaded() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.EMAIL_INPUT)))
            .as("Login page Email field should be visible")
            .isTrue();
        assertThat(isElementVisible(getElement(LoginPageLocators.PASSWORD_INPUT)))
            .as("Login page Password field should be visible")
            .isTrue();
        assertThat(isElementVisible(getElement(LoginPageLocators.LOGIN_BUTTON)))
            .as("Login page Login button should be visible")
            .isTrue();
        assertThat(isElementVisible(getElement(LoginPageLocators.REGISTER_LINK)))
            .as("Login page Register link should be visible")
            .isTrue();
    }

    public void verifyLoginSuccessful() {
        waitForPageLoad();
        String currentUrl = getCurrentUrl();
        assertThat(currentUrl)
            .as("User should be redirected to dashboard after successful login")
            .contains(LoginPageLocators.DASHBOARD_URL_FRAGMENT);
    }

    public void verifyAuthTokenPresent() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.AUTH_TOKEN_ELEMENT)))
            .as("Authentication token should be present after successful login")
            .isTrue();
    }

    public void verifyInvalidCredentialsError() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.INVALID_CREDENTIALS_ERROR)))
            .as("Invalid credentials error message should be displayed")
            .isTrue();
    }

    public void verifyEmptyUsernameError() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.EMPTY_USERNAME_ERROR)))
            .as("Empty username error message should be displayed")
            .isTrue();
    }

    public void verifyEmptyPasswordError() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.EMPTY_PASSWORD_ERROR)))
            .as("Empty password error message should be displayed")
            .isTrue();
    }

    public void verifyRequiredFieldError() {
        waitForPageLoad();
        assertThat(isElementVisible(getElement(LoginPageLocators.REQUIRED_FIELD_ERROR)))
            .as("Required field error message should be displayed")
            .isTrue();
    }

    public void verifyErrorMessageContains(String expectedText) {
        waitForPageLoad();
        WebElement errorElement = getElement(LoginPageLocators.ERROR_MESSAGE);
        assertThat(errorElement.getText())
            .as("Error message should contain expected text")
            .contains(expectedText);
    }

    public void verifyStatusCode(int expectedCode) {
        // For API-based login validation via underlying API call
        // This is a placeholder for backend verification if ExecutionAgent supports API validation
        // Actual implementation depends on test data structure
    }

    // ── Helper methods ────────────────────────────────────────────────────────

    private WebElement getElement(String locator) {
        // Try CSS first, then XPath
        try {
            return wait.until(ExpectedConditions.presenceOfElementLocated(By.cssSelector(locator)));
        } catch (Exception e) {
            return wait.until(ExpectedConditions.presenceOfElementLocated(By.xpath(locator)));
        }
    }
}
