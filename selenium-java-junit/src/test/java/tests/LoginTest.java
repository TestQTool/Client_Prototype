package tests;

import base.BaseTest;
import io.qameta.allure.*;
import org.junit.jupiter.api.*;
import pages.LoginPage;

import static org.junit.jupiter.api.Assertions.*;

/**
 * LoginTest - Generated by Qentrix ScriptGenerationAgent
 * Framework: Selenium Java JUnit
 * Test Cases: TC-560, TC-561, TC-562, TC-563, TC-564, TC-21, TC-22, TC-23, TC-24, TC-25
 */
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class LoginTest extends BaseTest {

    private LoginPage loginPage;

    @BeforeEach
    public void setUp() {
        super.setUp();
        loginPage = new LoginPage(driver);
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-560: Verify that login works with valid credentials
    // Tags: @smoke @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(1)
    @Tag("smoke")
    @Tag("regression")
    @Severity(SeverityLevel.CRITICAL)
    @Description("TC-560: Verify that login works with valid credentials")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc560_VerifyLoginWorksWithValidCredentials() {
        Allure.step("Send POST request to /login endpoint", () -> {
            loginPage.navigate();
        });

        Allure.step("Include valid username in request body", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Include valid password in request body", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Response should return 200 status code", () -> {
            loginPage.verifyStatusCode(200);
        });

        Allure.step("Validate response contains authentication token", () -> {
            loginPage.verifyAuthenticationToken();
            loginPage.verifyLoginSuccessful();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-561: Verify that login fails with invalid username
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(2)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Description("TC-561: Verify that login fails with invalid username")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc561_VerifyLoginFailsWithInvalidUsername() {
        Allure.step("Send POST request to /login endpoint", () -> {
            loginPage.navigate();
        });

        Allure.step("Include invalid username in request body", () -> {
            loginPage.enterUsername("invaliduser123");
        });

        Allure.step("Include valid password in request body", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Response should return 401 status code", () -> {
            loginPage.verifyStatusCode(401);
        });

        Allure.step("Validate error message in response", () -> {
            loginPage.verifyErrorMessageDisplayed();
            loginPage.verifyInvalidCredentialsError();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-562: Verify that login fails with invalid password
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(3)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Description("TC-562: Verify that login fails with invalid password")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc562_VerifyLoginFailsWithInvalidPassword() {
        Allure.step("Send POST request to /login endpoint", () -> {
            loginPage.navigate();
        });

        Allure.step("Include valid username in request body", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Include invalid password in request body", () -> {
            loginPage.enterPassword("wrongpassword123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Response should return 401 status code", () -> {
            loginPage.verifyStatusCode(401);
        });

        Allure.step("Validate error message in response", () -> {
            loginPage.verifyErrorMessageDisplayed();
            loginPage.verifyInvalidCredentialsError();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-563: Verify that login fails with empty username
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(4)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Description("TC-563: Verify that login fails with empty username")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc563_VerifyLoginFailsWithEmptyUsername() {
        Allure.step("Send POST request to /login endpoint", () -> {
            loginPage.navigate();
        });

        Allure.step("Include empty username field in request body", () -> {
            loginPage.clearUsername();
        });

        Allure.step("Include valid password in request body", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Response should return 400 status code", () -> {
            loginPage.verifyStatusCode(400);
        });

        Allure.step("Validate error message indicates missing username", () -> {
            loginPage.verifyRequiredFieldError();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-564: Verify that login fails with empty password
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(5)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Description("TC-564: Verify that login fails with empty password")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc564_VerifyLoginFailsWithEmptyPassword() {
        Allure.step("Send POST request to /login endpoint", () -> {
            loginPage.navigate();
        });

        Allure.step("Include valid username in request body", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Include empty password field in request body", () -> {
            loginPage.clearPassword();
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Response should return 400 status code", () -> {
            loginPage.verifyStatusCode(400);
        });

        Allure.step("Validate error message indicates missing password", () -> {
            loginPage.verifyRequiredFieldError();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-21: Verify that login works with valid credentials
    // Tags: @smoke @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(6)
    @Tag("smoke")
    @Tag("regression")
    @Severity(SeverityLevel.CRITICAL)
    @Description("TC-21: Verify that login works with valid credentials")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc21_VerifyLoginWorksWithValidCredentials() {
        Allure.step("Open login page", () -> {
            loginPage.navigate();
            loginPage.verifyLoginPageLoaded();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Click login button", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("User should login successfully", () -> {
            loginPage.verifyLoginSuccessful();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-22: To Test Login Form with Invalid Data
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(7)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Description("TC-22: To Test Login Form with Invalid Data")
    @Story("Login Functionality")
    @Feature("Authentication")
    public void tc22_TestLoginFormWithInvalidData() {
        Allure.step("Navigate to 192.168.10.124:4001", () -> {
            loginPage.navigate();
        });

        Allure.step("System should display a Login Page with the Email, Password fields, Login button and Register Hyperlink", () -> {
            loginPage.verifyLoginPageElements();
        });

        Allure.step("Enter username and password", () -> {
            loginPage.enterUsername("invaliduser");
            loginPage.enterPassword("invalidpass");
        });

        Allure.step("System should accept the username and password", () -> {
            loginPage.clickLoginButton();
            loginPage.verifyErrorMessageDisplayed();
        });
    }

    // ══════════════════════════════════════════════════════════════════════════
    // TC-23, TC-24, TC-25: Placeholder tests (no steps defined)
    // Tags: @regression
    // ══════════════════════════════════════════════════════════════════════════

    @Test
    @Order(8)
    @Tag("regression")
    @Severity(SeverityLevel.TRIVIAL)
    @Description("TC-23: To Test Login Form with Invalid Data - Placeholder")
    @Story("Login Functionality")
    @Feature("Authentication")
    @Disabled("No test steps defined - requires implementation")
    public void tc23_TestLoginFormWithInvalidDataPlaceholder() {
        Allure.step("Test case TC-23 requires step definition", () -> {
            // TODO: Implement when steps are provided
        });
    }

    @Test
    @Order(9)
    @Tag("regression")
    @Severity(SeverityLevel.TRIVIAL)
    @Description("TC-24: To Test Login Form with Invalid Data - Placeholder")
    @Story("Login Functionality")
    @Feature("Authentication")
    @Disabled("No test steps defined - requires implementation")
    public void tc24_TestLoginFormWithInvalidDataPlaceholder() {
        Allure.step("Test case TC-24 requires step definition", () -> {
            // TODO: Implement when steps are provided
        });
    }

    @Test
    @Order(10)
    @Tag("regression")
    @Severity(SeverityLevel.TRIVIAL)
    @Description("TC-25: test import 01 - Placeholder")
    @Story("Login Functionality")
    @Feature("Authentication")
    @Disabled("No test steps defined - requires implementation")
    public void tc25_TestImport01Placeholder() {
        Allure.step("Test case TC-25 requires step definition", () -> {
            // TODO: Implement when steps are provided
        });
    }
}
