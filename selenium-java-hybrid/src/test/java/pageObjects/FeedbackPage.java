package pageObjects;

/**
 * FeedbackPage Locators
 * Generated by Qentrix ScriptGenerationAgent
 * Rule: Locators only, zero logic, zero methods
 */
public class FeedbackPage {

    // ── Login elements ────────────────────────────────────────────────────────
    public static final String USERNAME_INPUT = "#user-name";
    public static final String PASSWORD_INPUT = "#password";
    public static final String LOGIN_BUTTON = "#login-button";
    public static final String ERROR_MESSAGE = "[data-test='error']";

    // ── Feedback form elements ────────────────────────────────────────────────
    // TODO: verify selector against live app
    public static final String FEEDBACK_TEXTAREA = "textarea[name='feedback']";
    // TODO: verify selector against live app
    public static final String FEEDBACK_TITLE_INPUT = "input[name='feedbackTitle']";
    // TODO: verify selector against live app
    public static final String FEEDBACK_CATEGORY_DROPDOWN = "select[name='category']";
    // TODO: verify selector against live app
    public static final String SUBMIT_FEEDBACK_BUTTON = "button[type='submit']";
    // TODO: verify selector against live app
    public static final String CANCEL_BUTTON = "button:contains('Cancel')";

    // ── Validation and messages ───────────────────────────────────────────────
    // TODO: verify selector against live app
    public static final String SUCCESS_MESSAGE = ".success-message";
    // TODO: verify selector against live app
    public static final String VALIDATION_ERROR = ".validation-error";
    // TODO: verify selector against live app
    public static final String REQUIRED_FIELD_ERROR = "span:contains('Required')";
    // TODO: verify selector against live app
    public static final String WHITESPACE_ERROR = "span:contains('invalid input')";
    // TODO: verify selector against live app
    public static final String MAX_LENGTH_ERROR = "span:contains('character limit exceeded')";

    // ── Form labels and placeholders ──────────────────────────────────────────
    // TODO: verify selector against live app
    public static final String FEEDBACK_LABEL = "label[for='feedback']";
    // TODO: verify selector against live app
    public static final String TITLE_LABEL = "label[for='feedbackTitle']";
    // TODO: verify selector against live app
    public static final String REQUIRED_INDICATOR = ".required-indicator";

    // ── Character counter ─────────────────────────────────────────────────────
    // TODO: verify selector against live app
    public static final String CHARACTER_COUNTER = ".character-counter";

    // ── API test endpoints (used in RestAssured tests if needed) ──────────────
    public static final String FEEDBACK_API_ENDPOINT = "/api/feedback";
    public static final String FEEDBACK_LIST_API_ENDPOINT = "/api/feedback/list";
}
