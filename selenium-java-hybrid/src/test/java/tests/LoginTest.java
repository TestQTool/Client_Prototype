package tests;

import base.BaseTest;
import io.qameta.allure.*;
import org.junit.jupiter.api.*;
import pages.LoginPage;

/**
 * LoginTest - Generated test cases for Login feature
 * Generated by: Qentrix ScriptGenerationAgent
 * Framework: Selenium + Java + JUnit5 + Allure
 */
@Epic("Authentication")
@Feature("Login")
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class LoginTest extends BaseTest {

    private LoginPage loginPage;

    @BeforeEach
    public void setUp() {
        super.setUp();
        loginPage = new LoginPage(driver);
    }

    // ── @smoke — Critical path ────────────────────────────────────────────────

    @Test
    @Order(1)
    @Tag("smoke")
    @Tag("regression")
    @Severity(SeverityLevel.BLOCKER)
    @Story("TC-560: Verify that login works with valid credentials")
    @Description("Send POST request to /login endpoint with valid username and password, verify 200 status and authentication token")
    public void tc560_VerifyLoginWorksWithValidCredentials() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page displays with all UI elements", () -> {
            loginPage.verifyLoginPageDisplayed();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Click login button", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify login success - 200 status equivalent", () -> {
            loginPage.verify200StatusCode();
        });

        Allure.step("Verify authentication token is present", () -> {
            loginPage.verifyAuthTokenPresent();
        });
    }

    @Test
    @Order(2)
    @Tag("smoke")
    @Tag("regression")
    @Severity(SeverityLevel.CRITICAL)
    @Story("TC-21: Verify that login works with valid credentials")
    @Description("Open login page, enter valid credentials, verify successful login")
    public void tc21_VerifyLoginWorksWithValidCredentials() {
        Allure.step("Open login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page loaded", () -> {
            loginPage.verifyLoginPageDisplayed();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Click login button", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify user logged in successfully", () -> {
            loginPage.verifyLoginSuccess();
        });
    }

    // ── @regression — Invalid credentials tests ───────────────────────────────

    @Test
    @Order(3)
    @Tag("regression")
    @Severity(SeverityLevel.CRITICAL)
    @Story("TC-561: Verify that login fails with invalid username")
    @Description("Send POST request with invalid username and valid password, verify 401 status and error message")
    public void tc561_VerifyLoginFailsWithInvalidUsername() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter invalid username", () -> {
            loginPage.enterUsername("invaliduser");
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify 401 status code equivalent", () -> {
            loginPage.verify401StatusCode();
        });

        Allure.step("Verify error message indicates invalid credentials", () -> {
            loginPage.verifyInvalidCredentialsError();
        });
    }

    @Test
    @Order(4)
    @Tag("regression")
    @Severity(SeverityLevel.CRITICAL)
    @Story("TC-562: Verify that login fails with invalid password")
    @Description("Send POST request with valid username and invalid password, verify 401 status and error message")
    public void tc562_VerifyLoginFailsWithInvalidPassword() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Enter invalid password", () -> {
            loginPage.enterPassword("wrongpassword");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify 401 status code equivalent", () -> {
            loginPage.verify401StatusCode();
        });

        Allure.step("Verify error message indicates invalid credentials", () -> {
            loginPage.verifyInvalidCredentialsError();
        });
    }

    @Test
    @Order(5)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-22: To Test Login Form with Invalid Data")
    @Description("Navigate to login page, enter invalid credentials, verify error handling")
    public void tc22_TestLoginFormWithInvalidData() {
        Allure.step("Navigate to 192.168.10.124:4001", () -> {
            loginPage.navigate();
        });

        Allure.step("Verify login page displays with Email, Password fields, Login button and Register hyperlink", () -> {
            loginPage.verifyLoginPageDisplayed();
        });

        Allure.step("Enter invalid username and password", () -> {
            loginPage.enterUsername("invaliduser");
            loginPage.enterPassword("invalidpass");
        });

        Allure.step("Submit login form", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify invalid credentials error", () -> {
            loginPage.verifyInvalidCredentialsError();
        });
    }

    // ── @regression — Empty field validation tests ────────────────────────────

    @Test
    @Order(6)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-563: Verify that login fails with empty username")
    @Description("Send POST request with empty username and valid password, verify 400 status and missing username error")
    public void tc563_VerifyLoginFailsWithEmptyUsername() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Leave username field empty", () -> {
            // Do not fill username
        });

        Allure.step("Enter valid password", () -> {
            loginPage.enterPassword("admin123");
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify 400 status code equivalent", () -> {
            loginPage.verify400StatusCode();
        });

        Allure.step("Verify error message indicates missing username", () -> {
            loginPage.verifyMissingUsernameError();
        });
    }

    @Test
    @Order(7)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-564: Verify that login fails with empty password")
    @Description("Send POST request with valid username and empty password, verify 400 status and missing password error")
    public void tc564_VerifyLoginFailsWithEmptyPassword() {
        Allure.step("Navigate to login page", () -> {
            loginPage.navigate();
        });

        Allure.step("Enter valid username", () -> {
            loginPage.enterUsername("admin");
        });

        Allure.step("Leave password field empty", () -> {
            // Do not fill password
        });

        Allure.step("Submit authentication request", () -> {
            loginPage.clickLoginButton();
        });

        Allure.step("Verify 400 status code equivalent", () -> {
            loginPage.verify400StatusCode();
        });

        Allure.step("Verify error message indicates missing password", () -> {
            loginPage.verifyMissingPasswordError();
        });
    }

    // ── Placeholder tests for incomplete test cases ───────────────────────────

    @Test
    @Order(8)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-24: To Test Login Form with Invalid Data")
    @Description("Test case has no steps defined - placeholder test")
    @Disabled("Test case TC-24 has no steps defined in test inventory")
    public void tc24_TestLoginFormWithInvalidData_Placeholder() {
        // No steps provided in test inventory
    }

    @Test
    @Order(9)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-25: test import 01")
    @Description("Test case has no steps defined - placeholder test")
    @Disabled("Test case TC-25 has no steps defined in test inventory")
    public void tc25_TestImport01_Placeholder() {
        // No steps provided in test inventory
    }

    @Test
    @Order(10)
    @Tag("regression")
    @Severity(SeverityLevel.NORMAL)
    @Story("TC-23: To Test Login Form with Invalid Data")
    @Description("Test case has no steps defined - placeholder test")
    @Disabled("Test case TC-23 has no steps defined in test inventory")
    public void tc23_TestLoginFormWithInvalidData_Placeholder() {
        // No steps provided in test inventory
    }
}
